---
import { whoIAmItems } from "@/components/about/data/whoIAmItems";
---

<div class="flex flex-col gap-3 xl:gap-6">
  {
    whoIAmItems.map((data) => {
      const {
        icon: { Icon, size },
        description,
        title,
      } = data;
      return (
        <div class="relative p-6 rounded-2xl shadow-[var(--shadow-white-sm)] transition-transform duration-500 hover:scale-105">
          <h3 class="font-bold mb-2 text-lg xl:text-2xl">{title}</h3>
          <p
            set:html={description}
            class="text-gray-300 leading-relaxed text-justify text-pretty text-sm xl:text-base"
          />
          <Icon class={`absolute top-2 right-2 ${size}`} />
        </div>
      );
    })
  }
</div>
